- go to directory material-applications/scaling-exercise
- docker-compose up
- in browser browse to localhost:3000 and press solution test button
- docker-compose down
- docker-compose up --scale compute=5
- in browser browse to localhost:3000 and press solution test button -> turns green
