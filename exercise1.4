docker run -d -it --name inputWebsite ubuntu sh -c "echo 'Input website:'; read website; echo 'Searching..'; sleep 1; curl http://$website;"
docker exec inputWebsite sh -c "apt-get update && apt-get -y install curl"
docker attach inputWebsite
helsinki.fi
